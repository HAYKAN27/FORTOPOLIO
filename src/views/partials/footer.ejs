<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <!-- Professional Footer -->
<footer class="bg-gray-900 dark:bg-black text-gray-300 pt-12 pb-8 mt-20">
    <div class="container mx-auto px-4 md:px-6">
        <!-- Main Footer Content -->
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
            
            <!-- Company/Personal Info -->
            <div class="space-y-4">
                <div class="flex items-center space-x-2">
                    <div class="w-10 h-10 bg-gradient-to-r from-blue-600 to-purple-600 rounded-lg flex items-center justify-center">
                        <i class="fas fa-code text-white text-xl"></i>
                    </div>
                    <span class="text-xl font-bold text-white">Portfolio<span class="text-blue-400">.dev</span></span>
                </div>
                <p class="text-gray-400 text-sm leading-relaxed">
                    Web Developer khusus dalam membangun solusi digital untuk UMKM dan startup. 
                    Fokus pada performa, desain modern, dan user experience terbaik.
                </p>
                <div class="flex space-x-4">
                    <a href="#" class="w-10 h-10 bg-gray-800 hover:bg-blue-600 rounded-full flex items-center justify-center transition-colors duration-300">
                        <i class="fab fa-linkedin-in"></i>
                    </a>
                    <a href="#" class="w-10 h-10 bg-gray-800 hover:bg-gray-700 rounded-full flex items-center justify-center transition-colors duration-300">
                        <i class="fab fa-github"></i>
                    </a>
                    <a href="#" class="w-10 h-10 bg-gray-800 hover:bg-pink-600 rounded-full flex items-center justify-center transition-colors duration-300">
                        <i class="fab fa-instagram"></i>
                    </a>
                    <a href="#" class="w-10 h-10 bg-gray-800 hover:bg-blue-500 rounded-full flex items-center justify-center transition-colors duration-300">
                        <i class="fab fa-twitter"></i>
                    </a>
                </div>
            </div>

            <!-- Quick Links -->
            <div>
                <h3 class="text-white text-lg font-semibold mb-6 pb-2 border-b border-gray-800">Quick Links</h3>
                <ul class="space-y-3">
                    <li>
                        <a href="#home" class="flex items-center text-gray-400 hover:text-white transition-colors duration-300 group">
                            <i class="fas fa-chevron-right text-xs text-blue-400 mr-2 group-hover:translate-x-1 transition-transform duration-300"></i>
                            Home
                        </a>
                    </li>
                    <li>
                        <a href="#about" class="flex items-center text-gray-400 hover:text-white transition-colors duration-300 group">
                            <i class="fas fa-chevron-right text-xs text-blue-400 mr-2 group-hover:translate-x-1 transition-transform duration-300"></i>
                            About Me
                        </a>
                    </li>
                    <li>
                        <a href="#portfolio" class="flex items-center text-gray-400 hover:text-white transition-colors duration-300 group">
                            <i class="fas fa-chevron-right text-xs text-blue-400 mr-2 group-hover:translate-x-1 transition-transform duration-300"></i>
                            Portfolio
                        </a>
                    </li>
                    <li>
                        <a href="#gallery" class="flex items-center text-gray-400 hover:text-white transition-colors duration-300 group">
                            <i class="fas fa-chevron-right text-xs text-blue-400 mr-2 group-hover:translate-x-1 transition-transform duration-300"></i>
                            Gallery
                        </a>
                    </li>
                    <li>
                        <a href="#experience" class="flex items-center text-gray-400 hover:text-white transition-colors duration-300 group">
                            <i class="fas fa-chevron-right text-xs text-blue-400 mr-2 group-hover:translate-x-1 transition-transform duration-300"></i>
                            Experience
                        </a>
                    </li>
                </ul>
            </div>

            <!-- Services -->
            <div>
                <h3 class="text-white text-lg font-semibold mb-6 pb-2 border-b border-gray-800">Layanan</h3>
                <ul class="space-y-3">
                    <li>
                        <a href="#" class="flex items-center text-gray-400 hover:text-white transition-colors duration-300 group">
                            <i class="fas fa-laptop-code text-blue-400 mr-3 text-sm"></i>
                            <span>Website Development</span>
                        </a>
                    </li>
                    <li>
                        <a href="#" class="flex items-center text-gray-400 hover:text-white transition-colors duration-300 group">
                            <i class="fas fa-palette text-purple-400 mr-3 text-sm"></i>
                            <span>UI/UX Design</span>
                        </a>
                    </li>
                    <li>
                        <a href="#" class="flex items-center text-gray-400 hover:text-white transition-colors duration-300 group">
                            <i class="fas fa-shopping-cart text-green-400 mr-3 text-sm"></i>
                            <span>E-commerce Solutions</span>
                        </a>
                    </li>
                    <li>
                        <a href="#" class="flex items-center text-gray-400 hover:text-white transition-colors duration-300 group">
                            <i class="fas fa-mobile-alt text-yellow-400 mr-3 text-sm"></i>
                            <span>Responsive Design</span>
                        </a>
                    </li>
                    <li>
                        <a href="#" class="flex items-center text-gray-400 hover:text-white transition-colors duration-300 group">
                            <i class="fas fa-rocket text-red-400 mr-3 text-sm"></i>
                            <span>Website Optimization</span>
                        </a>
                    </li>
                </ul>
            </div>

            <!-- Contact Info -->
            <div>
                <h3 class="text-white text-lg font-semibold mb-6 pb-2 border-b border-gray-800">Kontak</h3>
                <ul class="space-y-4">
                    <li class="flex items-start">
                        <i class="fas fa-envelope text-blue-400 mt-1 mr-3"></i>
                        <div>
                            <span class="block text-sm text-gray-400">Email</span>
                            <a href="mailto:hello@portfolio.dev" class="text-white hover:text-blue-400 transition-colors duration-300">
                                haykansyahputra27@gmail.com
                            </a>
                        </div>
                    </li>
                    <li class="flex items-start">
                        <i class="fas fa-phone text-green-400 mt-1 mr-3"></i>
                        <div>
                            <span class="block text-sm text-gray-400">Telepon/WhatsApp</span>
                            <a href="https://wa.me/6281234567890" class="text-white hover:text-green-400 transition-colors duration-300">
                                +62 882-9401-1161
                            </a>
                        </div>
                    </li>
                    <li class="flex items-start">
                        <i class="fas fa-map-marker-alt text-red-400 mt-1 mr-3"></i>
                        <div>
                            <span class="block text-sm text-gray-400">Lokasi</span>
                            <span class="text-white">Karawang, Indonesia</span>
                        </div>
                    </li>
                </ul>
            </div>
        </div>
        
        <!-- Copyright & Bottom Bar -->
        <div class="pt-8 border-t border-gray-800">
            <div class="flex flex-col md:flex-row items-center justify-between gap-4">
                <!-- Copyright -->
                <div class="text-center md:text-left">
                    <p class="text-gray-500 text-sm">
                        &copy; <span id="currentYear"></span> Portfolio.MHS. All rights reserved.
                        <span class="hidden md:inline"> | Made with <i class="fas fa-heart text-red-400 mx-1"></i> in Indonesia</span>
                    </p>
                </div>

                <!-- Tech Stack Badges -->
                <div class="flex items-center space-x-4">
                    <span class="text-xs text-gray-500 hidden md:block">Powered by:</span>
                    <div class="flex items-center space-x-2">
                        <span class="px-3 py-1 bg-gray-800 text-gray-300 text-xs rounded-full flex items-center">
                            <i class="fab fa-react text-blue-400 mr-1"></i> React
                        </span>
                        <span class="px-3 py-1 bg-gray-800 text-gray-300 text-xs rounded-full flex items-center">
                            <i class="fab fa-node-js text-green-400 mr-1"></i> Node.js
                        </span>
                        <span class="px-3 py-1 bg-gray-800 text-gray-300 text-xs rounded-full flex items-center">
                            <i class="fab fa-laravel text-red-400 mr-1"></i> Laravel
                        </span>
                    </div>
                </div>

                <!-- Legal Links -->
                <div class="flex items-center space-x-6">
                    <a href="#" class="text-gray-500 hover:text-white text-sm transition-colors duration-300">Privacy Policy</a>
                    <a href="#" class="text-gray-500 hover:text-white text-sm transition-colors duration-300">Terms of Service</a>
                    <a href="#" class="text-gray-500 hover:text-white text-sm transition-colors duration-300">Sitemap</a>
                </div>
            </div>

            <!-- Mobile Copyright -->
            <div class="mt-4 text-center md:hidden">
                <p class="text-gray-500 text-sm">
                    Made with <i class="fas fa-heart text-red-400 mx-1"></i> in Indonesia
                </p>
            </div>
        </div>
    </div>

    <!-- Back to Top Button -->
    <button id="backToTop" class="fixed bottom-6 right-6 w-12 h-12 bg-gradient-to-r from-blue-600 to-purple-600 text-white rounded-full shadow-lg hover:shadow-xl transition-all duration-300 opacity-0 translate-y-10 hidden md:flex items-center justify-center z-40">
        <i class="fas fa-chevron-up"></i>
    </button>
<!-- Footer JavaScript -->
<script>
    document.addEventListener('DOMContentLoaded', function() {
        // Set current year in copyright
        document.getElementById('currentYear').textContent = new Date().getFullYear();

        // Back to Top functionality
        const backToTopBtn = document.getElementById('backToTop');
        

        function toggleBackToTop() {
            if (window.scrollY > 300) {
                backToTopBtn.classList.remove('opacity-0', 'translate-y-10');
                backToTopBtn.classList.add('opacity-100', 'translate-y-0');
            } else {
                backToTopBtn.classList.remove('opacity-100', 'translate-y-0');
                backToTopBtn.classList.add('opacity-0', 'translate-y-10');
            }
        }

        // Initial check
        toggleBackToTop();

        // Listen to scroll events
        window.addEventListener('scroll', toggleBackToTop);

        // Smooth scroll to top
        backToTopBtn.addEventListener('click', function(e) {
            e.preventDefault();
            window.scrollTo({
                top: 0,
                behavior: 'smooth'
            });
        });

        // Smooth scroll for footer links
        document.querySelectorAll('footer a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function(e) {
                e.preventDefault();
                const targetId = this.getAttribute('href');
                if (targetId === '#') return;
                
                const targetElement = document.querySelector(targetId);
                if (targetElement) {
                    window.scrollTo({
                        top: targetElement.offsetTop - 80,
                        behavior: 'smooth'
                    });
                }
            });
        });

        // Newsletter form submission
        const newsletterForm = document.querySelector('footer form');
        if (newsletterForm) {
            newsletterForm.addEventListener('submit', function(e) {
                e.preventDefault();
                const emailInput = this.querySelector('input[type="email"]');
                const email = emailInput.value.trim();
                
                if (email && validateEmail(email)) {
                    // Simulate submission
                    const submitBtn = this.querySelector('button[type="submit"]');
                    const originalText = submitBtn.innerHTML;
                    
                    submitBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i> Subscribing...';
                    submitBtn.disabled = true;
                    
                    // Simulate API call
                    setTimeout(() => {
                        alert('Terima kasih! Anda telah berlangganan newsletter kami.');
                        emailInput.value = '';
                        submitBtn.innerHTML = originalText;
                        submitBtn.disabled = false;
                    }, 1500);
                } else {
                    alert('Silakan masukkan alamat email yang valid.');
                    emailInput.focus();
                }
            });
        }

        // Email validation
        function validateEmail(email) {
            const re = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
            return re.test(email);
        }

        // Mobile responsive adjustments
        function adjustFooterForMobile() {
            const whatsappBtn = document.querySelector('a[href*="whatsapp"]');
            const backToTopBtn = document.getElementById('backToTop');
            
            if (window.innerWidth < 768) {
                // Adjust positioning for mobile
                if (whatsappBtn) {
                    whatsappBtn.style.bottom = '80px';
                    whatsappBtn.style.left = '20px';
                }
                if (backToTopBtn) {
                    backToTopBtn.style.bottom = '20px';
                    backToTopBtn.style.right = '20px';
                }
            } else {
                // Reset for desktop
                if (whatsappBtn) {
                    whatsappBtn.style.bottom = '';
                    whatsappBtn.style.left = '';
                }
                if (backToTopBtn) {
                    backToTopBtn.style.bottom = '';
                    backToTopBtn.style.right = '';
                }
            }
        }

        // Initialize and listen for resize
        adjustFooterForMobile();
        window.addEventListener('resize', adjustFooterForMobile);

        // Hover effects for service icons
        const serviceLinks = document.querySelectorAll('footer .fa-laptop-code, footer .fa-palette, footer .fa-shopping-cart, footer .fa-mobile-alt, footer .fa-rocket');
        serviceLinks.forEach(icon => {
            icon.parentElement.addEventListener('mouseenter', function() {
                this.style.transform = 'translateX(5px)';
            });
            
            icon.parentElement.addEventListener('mouseleave', function() {
                this.style.transform = 'translateX(0)';
            });
        });

        // Dark mode toggle for footer (if you have one)
        const themeToggle = document.querySelector('[data-theme-toggle]');
        if (themeToggle) {
            themeToggle.addEventListener('click', function() {
                // Add transition effect to footer
                document.querySelector('footer').classList.add('transition-colors', 'duration-500');
            });
        }
    });
</script>

<!-- Footer Styles -->
<style>
    /* Custom scrollbar for footer */
    footer *::-webkit-scrollbar {
        width: 6px;
    }
    
    footer *::-webkit-scrollbar-track {
        background: #374151;
    }
    
    footer *::-webkit-scrollbar-thumb {
        background: #4B5563;
        border-radius: 3px;
    }
    
    footer *::-webkit-scrollbar-thumb:hover {
        background: #6B7280;
    }

    /* Animation for footer elements */
    @keyframes fadeInUp {
        from {
            opacity: 0;
            transform: translateY(20px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    footer > div > * {
        animation: fadeInUp 0.6s ease-out forwards;
    }

    /* Staggered animation for columns */
    footer .grid > *:nth-child(1) { animation-delay: 0.1s; }
    footer .grid > *:nth-child(2) { animation-delay: 0.2s; }
    footer .grid > *:nth-child(3) { animation-delay: 0.3s; }
    footer .grid > *:nth-child(4) { animation-delay: 0.4s; }

    /* Glow effect for social icons */
    footer .bg-gray-800:hover {
        box-shadow: 0 4px 15px rgba(59, 130, 246, 0.3);
    }

    /* Gradient border for newsletter */
    footer .bg-gradient-to-r {
        position: relative;
    }
    
    footer .bg-gradient-to-r::before {
        content: '';
        position: absolute;
        top: -2px;
        left: -2px;
        right: -2px;
        bottom: -2px;
        background: linear-gradient(45deg, #3B82F6, #8B5CF6, #EC4899);
        border-radius: 14px;
        z-index: -1;
        opacity: 0;
        transition: opacity 0.3s ease;
    }
    
    footer .bg-gradient-to-r:hover::before {
        opacity: 1;
    }

    /* Pulse animation for WhatsApp button */
    @keyframes pulse {
        0% {
            box-shadow: 0 0 0 0 rgba(34, 197, 94, 0.7);
        }
        70% {
            box-shadow: 0 0 0 10px rgba(34, 197, 94, 0);
        }
        100% {
            box-shadow: 0 0 0 0 rgba(34, 197, 94, 0);
        }
    }

    a[href*="whatsapp"] {
        animation: pulse 2s infinite;
    }

    /* Mobile optimizations */
    @media (max-width: 768px) {
        footer {
            padding-top: 3rem;
            padding-bottom: 2rem;
        }
        
        footer .grid {
            gap: 2rem;
        }
        
        footer h3 {
            font-size: 1.125rem;
        }
        
        /* Adjust floating buttons for mobile */
        #backToTop {
            width: 44px;
            height: 44px;
            bottom: 80px;
            right: 20px;
        }
        
        a[href*="whatsapp"] {
            width: 52px;
            height: 52px;
            bottom: 20px;
            left: 20px;
        }
    }

    @media (max-width: 640px) {
        footer .flex-col.sm\:flex-row {
            flex-direction: column;
        }
        
        footer form input {
            width: 100%;
            margin-bottom: 0.75rem;
        }
        
        footer form button {
            width: 100%;
        }
    }
</style>
</body>
</html>